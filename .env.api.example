# Dedicated API process: https://api-roomie.2startup.cloud
NEXT_PUBLIC_BASE_PATH="/"
NEXT_PUBLIC_API_URL="https://api-roomie.2startup.cloud"

DATABASE_URL="postgresql://roomie_app:<DB-PASSWORD>@127.0.0.1:5432/db_roomie"
DIRECT_URL="postgresql://roomie_app:<DB-PASSWORD>@127.0.0.1:5432/db_roomie"

ROOMIE_ACCESS_CODE="<PRODUCTION-ADMIN-ACCESS-CODE>"
ROOMIE_AUTH_SECRET="<LONG-RANDOM-PRODUCTION-SECRET>"
ROOMIE_UPLOAD_DIR="/var/lib/2startup/roomie/uploads"

# The API is consumed through the Vercel same-origin rewrite and the Hostinger
# /roomie proxy, so its session cookie must be valid for every API path.
ROOMIE_API_ONLY=1
ROOMIE_COOKIE_PATH=/
ROOMIE_COOKIE_SAME_SITE="lax"
# Production origins. `2startup-roomie.vercel.app` is the Vercel production
# frontend; `2startup.cloud` is the Hostinger frontend. Keep the retired
# `roomie-iota-beryl.vercel.app` entry only until the cutover is verified,
# then remove it and restart `roomie-api --update-env`.
ROOMIE_CORS_ORIGINS="https://2startup-roomie.vercel.app,https://roomie-iota-beryl.vercel.app,https://2startup.cloud"
